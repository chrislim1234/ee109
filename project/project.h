//extern enum states { Pause, Play };
//extern volatile unsigned long p = 0 ;
//extern volatile unsigned char timer = 0;  // Flag for state change
//extern volatile unsigned char state = Pause;
//extern unsigned char temp = 0;
extern int temp;
extern unsigned char* note;
extern int notename;
extern volatile unsigned char button;
extern volatile char state;
extern unsigned int freq;
